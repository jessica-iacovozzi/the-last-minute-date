
<div id="message-<%= message.id %>">
  <small>
  <div>
    <h3 class="show-message-firstname">
      <%= message.sender.first_name.capitalize %>
    </h3><i> ~ <%= message.created_at.strftime("%l:%M %p") %></i>
  </div>

    <p><%= message.content %></p>
  </small>

</div>
